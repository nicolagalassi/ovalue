---
context: default
status: in_progress
last_updated: 2026-06-14T10:11:23Z
---

# Stato sessione — OValue cleanup & feature

Nessun piano GSD attivo. Lavoro di manutenzione iterativo sulla codebase Vue 3 SPA.

<current_state>
Pulizia feature + spostamento CTA install script completati. Modifiche non ancora committate.
</current_state>

<completed_work>

- Rimossa newsfeed OGame: `NewsFeed.vue`, `rssFeedService.js`, import/ref/template in `Home.vue`
- Messe in pausa le Expirations: rotta `/expirations` rimossa da router e Header, `ExpirationsView.vue` conservata su disco
- Aggiunta ricerca per nome item nella Shopping List:
  - Campo nel sidebar desktop (con `aria-label`, `placeholder-slate-500`)
  - Campo mobile visibile solo `lg:hidden` in `catalog-header`
  - `searchResults` computed: cerca su tutti i SHOP_ITEMS, indipendente da evento/categoria attivi
  - Chiavi i18n: `shop_search_placeholder`, `shop_search_clear`, `shop_search_results`, `shop_search_no_results` in IT/EN/DE/FR
- Check impeccable UI/UX sui file modificati:
  - Rimosso import `computed` inutilizzato da `Home.vue`
  - Contrasto placeholder portato a `slate-500`; aria-label corretti
  - Warning Header.vue erano falsi positivi (opacity bg ~0.07)
- Rimossa firma forum completamente:
  - Eliminati: `ForumSignature.vue`, `useForumSignature.js`, `signatureService.js`
  - Rimossi da `SettingsView.vue`: import, `useOgameFormulas`, 5 computed `sig*`, blocco template firma
  - Rimosso da `MetalCalc.vue`: `maxMetalMine` computed (era solo per la firma)
  - Rimosso `showIntro` ref e pannello intro da `MetalCalc.vue`
  - Pulizia 38 chiavi `sig_*` + `settings_nickname_desc` da tutti e 4 i locale
- Spostata CTA install script da MetalCalc a Home:
  - `Home.vue`: blocco install visibile solo per `!activeProfile` (sotto `index_desc`)
  - Aggiunta chiave `home_install_hint` in IT/EN/DE/FR
  - `MetalCalc.vue`: pannello intro rimosso completamente

Tutte le build sono state verificate ✓.
</completed_work>

<remaining_work>

- Committare le modifiche non ancora in git:
  - `src/components/ForumSignature.vue` (deleted)
  - `src/composables/useForumSignature.js` (deleted)
  - `src/services/signatureService.js` (deleted)
  - `src/locales/de.json`, `en.json`, `fr.json`, `it.json` (modified)
  - `src/views/Home.vue`, `MetalCalc.vue`, `SettingsView.vue` (modified)
- Verificare visivamente su localhost la CTA install script nella Home (nuovi utenti vs utenti con profilo)
- Valutare se il blocco `!activeProfile` nella Home necessita di un secondo step "poi sincronizza" più esplicito
</remaining_work>

<decisions_made>

- Newsfeed rimossa perché bloccata da proxy CORS in produzione (ovalue.net); troppo poco usata per giustificare una soluzione (es. Cloudflare Worker)
- Expirations "messa in pausa" non eliminata: `ExpirationsView.vue` conservata su disco, solo rotta/nav rimosse
- Ricerca shopping: campo nel sidebar desktop (coerente con gli altri filtri), mobile nel catalog-header con `lg:hidden`
- Firma rimossa completamente (feature poco usata, aggiungeva complessità e dipendenze)
- Pannello intro MetalCalc rimosso: il suo unico scopo era la discovery dello script, ora gestita dalla Home
</decisions_made>

<blockers>
Nessuno.
</blockers>

## Stato file non committati

```
D src/components/ForumSignature.vue
D src/composables/useForumSignature.js
D src/services/signatureService.js
M src/locales/de.json
M src/locales/en.json
M src/locales/fr.json
M src/locales/it.json
M src/views/Home.vue
M src/views/MetalCalc.vue
M src/views/SettingsView.vue
```

## Infrastruttura

- **Build**: Vite + Vue 3 SPA. `npm run build` → `dist/`. Build pulita verificata ✓
- **Dev server**: `npm run dev` (non attivo alla pausa)
- **PWA**: workbox generato automaticamente alla build

<context>
Sessione di manutenzione/cleanup. Nessuna fase GSD. Il filo era: alleggerire l'app rimuovendo feature poco usate (news, expirations, firma), aggiungere una feature utile (ricerca shopping), e assicurarsi che i nuovi utenti vedano subito il CTA per installare lo script exporter.
</context>

<next_action>
Committare le modifiche pendenti con un messaggio che copre: rimozione firma, spostamento CTA install, pulizia locale. Poi fare `npm run dev` e verificare visivamente la Home con e senza profilo attivo.
</next_action>
